spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/info_service_db #изменить адрес на свой
    username: postgres
    password: V #изменить пароль на свой
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  server:
    error:
      include-message: always

server:
  port: 8083 #изменить на свой сервис: credit service(8084), info service(8085), core(8081)